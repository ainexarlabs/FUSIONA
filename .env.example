# Client-side (Vite) — safe to expose in the browser bundle.
VITE_SUPABASE_URL=https://your-project-ref.supabase.co
VITE_SUPABASE_ANON_KEY=your-anon-key
VITE_WHATSAPP_NUMBER=5217226830208

# Server-side (Cloudflare Pages Functions) — set as secrets in Cloudflare,
# never in this file or the client bundle. See wrangler.toml for the full list.
